Placeholder PWA icons (solid brand-blue squares). Replace `icon-192.png`, `icon-512.png`,
and `icon-maskable-512.png` with real artwork before shipping — keep the same filenames
and sizes, or update `public/manifest.json` and `src/app/layout.tsx` metadata to match.
